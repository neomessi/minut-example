/**
 * mongoose definition - maybe they provide? I enforce. if none provided then anything can get inserted
 * 
 * as well as standard crud funcs getUser(id), etc. (it's security's job to pass _current_ user id)
 * 
 * idk maybe this should be userInfo.js - just deal with info?
 */